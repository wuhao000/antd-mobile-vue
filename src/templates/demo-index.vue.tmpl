<template>
  <div class="demo">
    <code-box :code="code"
              :markdown="md">
      <demo-comp slot="demo"/>
    </code-box>
  </div>
</template>
<script lang="ts">
  import source from './{{{demoName}}}.txt';
  import md from '@/packages/{{{dir}}}/demo/{{{demoName}}}/index.md';
  import Vue from 'vue';
  import Component from 'vue-class-component';
  import CodeBox from '../../components/code-box.vue';
  import DemoComp from '@/packages/{{{dir}}}/demo/{{{demoName}}}/index.vue';

  @Component({
    name: '{{{name}}}',
    components: {CodeBox, DemoComp}
 })
  export default class {{{name}}} extends Vue {
    public code = source;
    public md = md;
  }
</script>
